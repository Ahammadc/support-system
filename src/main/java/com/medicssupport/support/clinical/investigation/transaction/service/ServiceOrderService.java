package com.medicssupport.support.clinical.investigation.transaction.service;

import com.medicssupport.support.application.core.layer.SuperService;
import com.medicssupport.support.clinical.investigation.transaction.model.service_order.ServiceOrder;

public interface ServiceOrderService extends SuperService<ServiceOrder, Integer> {
}
